            if (player == 1) {
                elem.innerHTML = "_"
                elem.style.color = "red"
                gIndex++
                player = 2;
                turn++
                console.log(gravityTable);
            } else {
                elem.innerHTML = "}"
                elem.style.color = "yellow"
                player = 1;
                turn++
            }


                if (elem.innerHTML == "" && gameOver == false) {
        for (let i = 0; i < gravityTable.length; i+7) {
            if(gravityTable[i + 7] == "" && gravityTable[i] < 36)
            gravityTable[i] = gravityTable
        }


                        if (gravityTable[i + 7] && gravityTable[i + 7].innerHTML == "_" && gravityTable[i + 7].innerHTML == "}" && playerOne == true) {
                    gravityTable[i].innerHTML = "_"
                    elem.style.color = "red"
                    playerTwo = true;
                    turn++
                } else if (gravityTable[i + 7] && gravityTable[i + 7].innerHTML == "_" && gravityTable[i + 7].innerHTML == "}" && playerTwo == true) {
                    gravityTable[i].innerHTML = ""
                    elem.style.color = "yellow"
                    playerOne = true;
                    turn++
                }